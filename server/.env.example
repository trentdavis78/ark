# Read server-side only. This key must never ship inside the Android APK —
# that is the entire reason the vision read is an endpoint rather than an
# on-device SDK call.
ANTHROPIC_API_KEY=sk-ant-...
PORT=8787
