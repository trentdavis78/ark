{
  "name": "blacktop-vision",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vision extraction endpoint: reads a delivery offer card from a screenshot. Called by the Android client when node capture fails.",
  "scripts": {
    "dev": "node --experimental-strip-types src/dev-server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
